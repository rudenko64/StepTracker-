 class Converter {
    MonthData monthData = new MonthData();
    int convertToKm(int steps) {
        double distanceKm = MonthData.sumStepsFromMonth() * 0.00075 ;
    }
    int corvertStepsToKilocalories(int steps) {
        double kcal = MonthData.sumStepsFromMonth()/25;
    }
 }
